;;; Note loaders
(defun backups-notes()
  (interactive)
  (find-file "~/Documents/IT_misc/backup_drives.notes"))

(defun clients-notes()
  (interactive)
  (find-file "~/Documents/IT_misc/nicks_clients.notes"))

(defun dailyplanet-notes()
  (interactive)
  (find-file "~/Documents/IT_misc/dailyplanetva.notes"))

(defun domainjoin-notes()
  (interactive)
  (find-file "~/Documents/IT_misc/domain_join.org"))

(defun route-notes()
  (interactive)
  (find-file "~/Documents/IT_misc/adding_routes.notes"))

(defun wpkg-notes()
  (interactive)
  (find-file "~/Documents/IT_misc/wpkg.notes"))